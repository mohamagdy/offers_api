Fyber Offers API
================

# Running the app

1. Change the config in config/secrets.yml
2. Run the Rails server using `rails s`

# Running tests

* To run all the test suites, use the command `rspec`
* To run a specific test, use the command `rspec <spec-file-path>`